// use clap::Parser;
// use std::io::{BufRead, BufReader};

// #[derive(Parser)]
// #[command(name = "resplit")]
// #[command(version = "0.0.1")]
// #[command(author = "Alfredo Deza")]
// #[command(about = "Split strings by one or more delimiters and return a field, like cut")]
